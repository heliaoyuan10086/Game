ENTITY_ENTER
{
    --$obj1.countDown = false
    StartTimer(rep = true, time = 200, object = $obj1, event = "ADD_MONEY")
}

ADD_MONEY
{
    --If($obj1.countDown == true)
    --{
    --    AddCurrency(count = 100, coinName = "green_currency", entity = $obj1)
    --}

    --$obj1.countDown = true


    AddCurrency(count = 100, coinName = "green_currency", entity = $obj1)
}