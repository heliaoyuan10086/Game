REGION_ENTER {
    $tip = GetRegionConfig(key = "tip", region = $region)
	ShowRegionTip(player = $obj1, cfg = $tip, isShow = true)
}

REGION_LEAVE {
	$tip = GetRegionConfig(key = "tip", region = $region)
    ShowRegionTip(player = $obj1, cfg = $tip, isShow = false)
}
